#include <iostream>
using namespace std;

/*

Check compilation of g++
complete the code given below and run the following command:
g++ compile.cpp -o compile && ./compile && rm compile
Hopefully the terminal should output "Hello <name>!"

*/

int main() {
    const char *name = ""; // put your name inside the quotes
    cout<<"Hello "<<name<<"!"<<endl;
}